// Importing here rather than in main.ts keeps Vuetify's utility classes and icon font in the admin
// chunk, so the public signup page keeps its own styling.
import "@mdi/font/css/materialdesignicons.css";
import "vuetify/dist/vuetify.css";
